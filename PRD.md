# 해외의류 소싱 크롤러 PRD

작성일: 2026-09-03
참고 베이스: `byungjunjang/web-crawler` (범용 웹 크롤링 에이전트, MIT)

---

## 1. 목적과 전제

| 항목 | 결정 |
|---|---|
| 용도 | **재판매/셀러 소싱** (개인 구매용 아님) |
| 대상 지역 | 미국, 유럽, 일본 |
| 산출물 | 엑셀(.xlsx) + 반응형 웹 대시보드 |
| 핵심 질문 | "이 상품을 지금 떼오면 국내에서 얼마 남는가" |

### 이 도구의 정의
가격 수집기가 아니라 **소싱 판단 도구**다. 최종 출력은 상품 목록이 아니라
`마진율 순으로 정렬된 소싱 후보 리스트`다.

### 명시적 가정 (다르면 수정 필요)
- A1. 통관 형태는 **구매대행(proxy)과 사업자 사입(business) 둘 다** 지원한다. 설정 스위치로 전환.
- A2. 초기 국내 시세 비교 대상은 **네이버쇼핑 최저가**를 기준으로 한다.
- A3. 1인 운영 기준. 서버는 두지 않고 로컬 PC(Windows) + 스케줄러로 시작한다.
- A4. 수집 규모는 사이트당 1일 1회, 상품 수백~수천 건 수준.

---

## 2. 시스템 구조

```
[L1 수집층]  참고 레포 그대로 재사용
   Scrapling + fingerprints/<도메인>/profile.json (수집 레시피)
        │
[L2 정규화층]  ★신규
   사이트별 제각각인 필드 → 공통 Product 스키마
   브랜드명 표준화 / 사이즈 표준화(US·UK·EU·JP) / 스타일코드 추출
        │
[L3 원가층]  ★신규 — landed_cost.py (완성됨)
   현지세 처리 → 국제배송비 → 관부가세 → 실질환율 → 최종원가(원)
        │
[L4 시세매칭층]  ★신규 — 이 프로젝트의 최대 난제
   국내 판매가 수집(네이버쇼핑) → 스타일코드/브랜드+모델명으로 매칭
        │
[L5 저장층]  ★신규
   SQLite: products / price_history / domestic_price / sourcing_score
        │
[L6 출력층]  엑셀 + 웹 대시보드 + 알림
```

---

## 3. 데이터 스키마

### products (해외 수집 원본)
| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | TEXT PK | `{domain}:{site_product_id}` |
| domain / region | TEXT | US / EU / JP |
| brand / brand_norm | TEXT | brand_norm은 표준화된 브랜드명 |
| name | TEXT | |
| style_code | TEXT | **매칭 키. 최우선 확보 대상** |
| sku / color / size_raw / size_norm | TEXT | |
| list_price / sale_price / currency | REAL/TEXT | |
| in_stock / stock_qty | INT | 사이즈별 재고 |
| weight_kg | REAL | 없으면 카테고리 평균 추정 |
| image_url / url | TEXT | |
| crawled_at | TEXT | |

### price_history
`(product_id, date, sale_price, in_stock)` — 할인 감지·시즌 패턴 분석용.
이게 없으면 "지금이 싼 건지" 판단이 불가능하므로 1단계부터 넣는다.

### domestic_price
`(match_key, channel, price, seller, url, checked_at)`

### sourcing_score (뷰)
`landed_cost_krw`, `domestic_price_krw`, `profit_krw`, `margin_rate`, `confidence`
- `confidence`: 매칭 신뢰도(style_code 일치=high / 브랜드+모델명 유사=medium / 수동=manual)

---

## 4. 지역별 수집 전략

| 지역 | 1순위 대상 | 방법 | 주의점 |
|---|---|---|---|
| 미국 | Shopify 기반 브랜드몰 | `/products.json?limit=250&page=N` | 가장 쉬움. **여기서 시작** |
| 미국 | 대형 편집숍(SSENSE 등) | Chrome CDP | Akamai/PX. **MVP 제외** |
| 유럽 | 이탈리아 편집숍(Shopify/Magento 다수) | products.json 또는 API | **표시가 VAT 포함** → 반드시 차감 |
| 유럽 | Zalando | DynamicFetcher | 국가별 도메인마다 가격 상이 |
| 일본 | 라쿠텐 | **공식 개발자 API** | 크롤링 불필요. 무료 API 우선 확인 |
| 일본 | ZOZOTOWN | DynamicFetcher | 안티봇 약한 편. 세込/税抜 구분 필수 |

**착수 순서: 미국 Shopify → 일본 라쿠텐 API → 유럽 Shopify → 나머지**

---

## 5. 원가 계산 규칙 (landed_cost.py 구현 완료)

```
최종원가 = 상품실결제액 + 국제배송비 + 관세 + 부가세 + 통관수수료
```

| 처리 | 내용 |
|---|---|
| 유럽 VAT | 표시가 ÷ (1+VAT). **놓치면 원가가 20% 틀어짐** |
| 일본 소비세 | 세込 표시면 ÷1.1 (수출 면세) |
| 미국 판매세 | 면세 州 배대지면 0 |
| 실질환율 | 매매기준율 × (1 + 카드수수료 2.5%) |
| 면세한도 | proxy 모드만 적용. 미국발 $200 / 그 외 $150. 초과 시 **전액 과세** |
| 관세율 | 의류·신발 13%, 가방·시계 8%, 부가세 10% |
| 부가세 공제 | business 모드 + 매입세액공제 시 원가에서 제외 |

### 검증 결과 (동일 상품 $180 의류, 0.8kg)
| 모드 | 최종원가 |
|---|---|
| 구매대행(면세 적용) | 273,610원 |
| 사업자 사입 | 340,096원 |
| 사업자 사입 + 부가세 공제 | 309,179원 |

→ **같은 상품도 통관 형태에 따라 원가가 25% 차이난다.** 모드 설정이 마진 계산의 전제.

---

## 6. 구입처 자동 판정

여러 몰을 사전 수집해두고 같은 상품끼리 묶어 **최저가 몰을 구입처로 확정**한다.
실시간 검색이 아니라 사전 수집 + 매칭 방식이다.

| 순위 | 매칭 키 | 신뢰도 표기 |
|---|---|---|
| 1 | 제조사 품번(SKU) | 품번 일치 |
| 2 | 바코드(EAN/UPC) | 품번 일치 |
| 3 | 이미지 지문(pHash) | 이미지 일치 |
| 4 | 브랜드 + 상품명 유사도 | 이름 유사 — 확인 필요 |

3번이 실전에서 가장 잘 통한다. 편집숍들이 브랜드 룩북 이미지를 공유하기 때문에
품번이 없어도 같은 상품이 잡힌다.

**한계**: 수집 대상에 포함된 몰 안에서만 찾는다. 브랜드당 공식몰 + 아울렛 + 편집숍 3~4곳을
넣어두면 대부분 커버된다.

---

## 7. 검색 사양

브랜드 / 옷 종류 / 가격 중 **하나만 입력해도** 걸린다. 한 줄 입력을 파싱한다.

| 입력 | 인식 |
|---|---|
| `라코스테` | 브랜드 |
| `가디건` | 옷 종류 |
| `20만원 이하` | 최종 원가 기준 |
| `$200 이하` | 현지가 기준 |
| `쟈딕 가디건 40만원 이하` | 세 조건 동시 |
| `블랙` | 색상 (한글 가능) |

- 만원 단위는 **최종 원가** 기준, 달러는 **현지가** 기준
- 약칭 지원: `쟈딕`, `뉴발`, `골구`, `룰루`, `아크`
- 브랜드 별칭은 **긴 이름부터** 검사한다 (`라코스테`가 `코스`로 잡히지 않도록)
- `폴로`는 브랜드가 아니라 옷 종류로 처리한다

### 자동 추천 (조건 없을 때)

| 요소 | 가중치 |
|---|---|
| 할인율 | 기본 점수 |
| 브랜드 세일 개시 | +15 |
| 추가 세일 | +10 |
| 품목 확대 | +6 |
| 할인율 40% 이상 | +8 |
| 면세 구간 | +8 |

면세 여부에 점수를 준 이유는, 할인율이 같아도 관부가세가 없는 상품이 마진이 훨씬 좋기 때문이다.

**추후 추가**: 카페 24시간 조회수 기반 브랜드·가격대 반응도. 데이터 3~4주 축적 후.

---

## 8. 게시글 사양

```
제목: [해외직구] {♡핫딜♡} {브랜드 한글명} {제품명}
      할인율 40% 이상이면 ♡핫딜♡ 자동 부착
```

본문에 들어가는 항목: 브랜드, 상품명, 현지가(정가→세일가+할인율), 예상 판매가,
사이즈, 컬러, **구입처**, **구입처 주소**, 관부가세 안내, 문의 안내.

이미지는 수집분 중 앞 8장이 기본 선택되고, 탭으로 조정 후 한 번에 저장·공유한다.
공유 시트를 지원하는 기기에서는 카페 앱으로 바로 전달된다.

> 현재 본문 템플릿은 **추정본**이다. 실제 카페 글 형식을 확인해 교체할 것.

---

## 9. 폴더 구조

```
sourcing-crawler/
├── CLAUDE.md                  Claude Code용 프로젝트 맥락
├── README.md                  실행 방법
├── PRD.md                     이 문서
├── config/
│   ├── cost.yaml              ✅ 관세·환율·배대지·수수료
│   ├── targets.yaml           수집 대상 몰 목록
│   └── brand_alias.yaml       브랜드명 표준화 사전
├── crawlers/
│   ├── shopify.py             products.json 범용 수집기 (1주차 최우선)
│   ├── rakuten_api.py
│   └── generic.py             Scrapling 기반
├── core/
│   ├── landed_cost.py         ✅ 원가·마진 계산
│   ├── normalize.py           브랜드·사이즈·품번 정규화
│   ├── matcher.py             크로스몰 상품 매칭
│   └── signals.py             세일 급변 판정
├── storage/
│   └── db.py                  SQLite
├── output/
│   ├── excel.py
│   └── post.py                게시글 텍스트·이미지 패키지
└── web/
    └── index.html             ✅ 모바일 프론트
```

---

## 10. 로드맵

| 주차 | 범위 | 완료 기준 |
|---|---|---|
| 1주 | Shopify 범용 수집기 + 정규화 + landed_cost 연결 + 엑셀 | 미국 몰 5곳에서 상품 500건 → 최종원가 컬럼이 채워진 xlsx |
| 2주 | SQLite 이력 저장 + 일 1회 스케줄 + 가격하락 감지 | 전일 대비 할인 상품이 별도 시트로 분리됨 |
| 3주 | 국내 시세 매칭 + 마진 랭킹 | 마진율 정렬된 소싱 후보 Top 50 |
| 4주 | 웹 대시보드(반응형) + 알림 | 모바일에서 관심상품·목표마진 알림 확인 가능 |
| 이후 | 일본·유럽 확장, 안티봇 사이트 | |

---

## 11. 리스크

| 리스크 | 대응 |
|---|---|
| **상업적 스크래핑 ToS 위반** | 재판매 목적은 개인 참고보다 리스크가 크다. 공개 API·제휴 피드(어필리에이트 상품 피드)를 우선 사용. 수집은 저빈도·저동시성 |
| 국내 시세 매칭 실패 | style_code 없는 상품이 다수. confidence 등급을 두고 medium 이하는 수동 확인 큐로 |
| IP 차단 | 요청 간격 랜덤(2~5초), 도메인별 동시성 1~2 |
| 관세·요율 변동 | cost.yaml 분기별 재확인. `meta.last_verified` 갱신 |
| 재고 정보 신선도 | 소싱 판단은 재고가 생명. 상위 후보는 주문 직전 단건 재확인 로직 필요 |
| 개인정보 | 리뷰·판매자 정보 수집 금지. 상품 정보만 수집 |

---

## 12. 다음 결정 필요 항목

1. 통관 기본 모드: 구매대행 / 사업자 사입 (원가 25% 차이)
2. 배대지 업체 확정 → cost.yaml 요율표 실제값 교체
3. 판매 채널 (스마트스토어 / 쿠팡 / 자사몰) → 수수료율 확정
4. 1주차 수집 대상 브랜드몰 5~10곳 선정
